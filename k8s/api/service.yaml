apiVersion: v1
kind: Service
metadata:
  name: documents-api-service
  namespace: documents
spec:
  selector:
    app: documents-api
  ports:
  - port: 80
    targetPort: 3000
    nodePort: 30080
  type: NodePort