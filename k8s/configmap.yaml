apiVersion: v1
kind: ConfigMap
metadata:
  name: documents-config
  namespace: documents
data:
  DB_HOST: "postgres-service"
  DB_PORT: "5432"
  DB_NAME: "otus_documents_db"
  NODE_ENV: "production"
  PORT: "3000"
  HOST: "0.0.0.0"